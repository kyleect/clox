if (argc() != 4) {
  print "Usage: clox -f examples/cat.lox <file>";
  exit(1);
}

var path = argv(3);

if (!fileExists(path)) {
  print "File doesn't exist '" + path + "'";
  exit(1);
}

var text = readFileToString(path);

print text;
